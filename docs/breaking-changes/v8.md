# Breaking Changes in v8

- Adds support for `bun` and `pnpm`
  - Updates logic for existing `npm` and `yarn` detection, and so may inadvertently change behavior. Cutting as new major as a precaution.
